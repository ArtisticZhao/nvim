# ArtisticZhao's neovim configurations for Mac

## Install neovim
```
brew install neovim
```

## Requirements

- [node.js](https://nodejs.org/en/)
- pynvim  
  `python3 -m pip install pynvim --user`
- ctags  
  `brew install --HEAD universal-ctags/universal-ctags/universal-ctags`
